{% extends 'nav.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
<style>

    @import url('https://fonts.googleapis.com/css?family=Abel');

    .container{
        padding-top: 20px;
    }

    div#box{
        width: 100%;
        height: 2px;
        background-color: #7e80c9;
    }

    div#button-editar{
        padding-top: 25px;
    }

    ul#list-historias.list-group{
        padding-top: 30px;
    }

    div.overflow-hidden{
        height: 25px;
    }

    a#button-adicionarCap{
        color: white;
        background-color: #9452C7;
    }

</style>

{#<div class="example-wrapper">#}
{#    {% for item in historico.getCapitulo %}#}
{#        <a href="/capitulo/{{ item.getId }}">#}
{#            <p>{{ item.getIdHistoria }}</p>#}
{#            <p>{{ item.getTitulo }}</p>#}
{#        </a>#}
{#        <p>---------------------------</p>#}
{#    {% endfor %}#}
{#</div>#}
{#{% endblock %}#}

<div class="container">
    <h1>Histórico</h1>
    <div id="box"></div>

    <ul class="list-group" id="list-historias">
        {% for item in historico.getCapitulo %}
            <div class="card mb-3" style="width: auto">
                <div class="row no-gutters">
                    <div class="col-md-2">
                        <img src="http://2.bp.blogspot.com/-JytFHxae52w/VM0_FmMBREI/AAAAAAAAAJ8/nnony8vtisI/s1600/pacman.png" class="card-img" alt="...">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <a href="/historia/{{ item.getIdHistoria }}" class="card-title">{{ item.getTitulo }}</a>
{#                            <div class="overflow-hidden">{{ item.getSinopse }}</div>#}
{#                            <p class="card-text"><small class="text-muted">{{ item.getClassificacao }}</small></p>#}
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </ul>
</div>
{% endblock %}