{% extends 'nav.html.twig' %}

{% block title %}Genero{% endblock %}

{% block body %}

    <style>
        div#box{
            width: 100%;
            height: 2px;
            background-color: #9452c7;
        }

        div.container-fluid{
            padding: 40px 30px 10px 30px;
        }

        div#lista{
            padding-top: 30px;
        }
    </style>

    <div class="container-fluid">
        <h1>Historias</h1>
        <div id="box"></div>
        <div id="lista">
            <ul class="list-group">
                {% for historia in historias %}
                    <li class="d-flex justify-content-between">
                        <a href="/historia/{{ historia.id }}" class="list-group-item list-group-item-action">
                            {{ historia.titulo }}
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}
